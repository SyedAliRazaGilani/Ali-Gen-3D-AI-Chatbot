# Vite static site — source folder in repo: AliGen-frontend (after folder rename).
services:
  - name: AliGen-frontend
    type: web
    env: static
    buildCommand: "corepack enable && yarn install --immutable && yarn build"
    staticPublishPath: "dist"
    envVars:
      - key: NODE_VERSION
        value: 18
