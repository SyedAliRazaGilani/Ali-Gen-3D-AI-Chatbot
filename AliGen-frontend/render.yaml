# Vite static site — Render root directory should be this folder (AliGen-frontend).
services:
  - name: AliGen-frontend
    type: web
    env: static
    buildCommand: "corepack enable && yarn install --immutable && yarn build"
    staticPublishPath: "dist"
    envVars:
      - key: NODE_VERSION
        value: 18
